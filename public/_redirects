# Cloudflare Pages SPA Routing
# Single catch-all rule - Cloudflare automatically serves static assets
# No need to list every file type - just use the catch-all

# SPA fallback - all non-file routes serve index.html
/*    /index.html   200

