/**
 * lucideConfig.js — Single source of truth for Lucide icon defaults.
 * Spread into icon usages to ensure stroke consistency across the app.
 *
 * Usage:
 *   import { LUCIDE_DEFAULT_PROPS } from '../../utils/lucideConfig';
 *   <Home size={20} {...LUCIDE_DEFAULT_PROPS} />
 */

export const LUCIDE_DEFAULT_PROPS = {
  strokeWidth: 1.75,
};
