/**
 * lucideDefaults.js — Unified Lucide icon defaults.
 * Single source of truth for stroke weight and size across the app.
 *
 * Usage:
 *   import { LUCIDE_DEFAULTS } from '../../utils/lucideDefaults';
 *   <Icon {...LUCIDE_DEFAULTS} />
 *   <Icon {...LUCIDE_DEFAULTS} size={16} />   // override size only
 */

export const LUCIDE_DEFAULTS = {
  size: 24,
  strokeWidth: 2.2,
};
