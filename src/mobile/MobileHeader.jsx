/**
 * MobileHeader - REMOVED
 *
 * One Header Rule:
 * - Global navbar is the only header
 * - Page context lives inside content (via PageTitle component)
 * - Eliminates duplicate mobile banners
 * - Reduces vertical clutter
 */
export default function MobileHeader() {
  return null;
}

